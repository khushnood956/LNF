# Components

Reusable React components for the application.

## Structure
- UI components (Button, Card, Modal, etc.)
- Feature components (ItemCard, SearchBar, etc.)
- Layout components (Header, Footer, Sidebar, etc.)
